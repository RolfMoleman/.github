# Local font dependency

Place a locally licensed copy of `stonehen.ttf` here, or pass its path with
`--font` when running the renderer.

The font file is intentionally ignored by Git and must not be committed without
documented redistribution rights.
